# Copy to .env for local development. Never commit real credentials.
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/pipelineinsights

# The scheduler remains disabled until the live-testing checkpoint.
SCHEDULER_ENABLED=false
DEMO_DATA=true